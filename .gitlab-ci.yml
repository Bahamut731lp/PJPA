image: python:latest

stages:
    - test

run_tests:
    stage: test
    before_script:
        - pip install -r requirements.txt
    script:
        - pytest -v

run_linter:
    stage: test
    script:
        py -m pylint